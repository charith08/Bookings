<% provide(:title, "Booking Details") %>

<h1>Booking Details</h1>
<div>

<p>
  <strong>Room:</strong>
  <%= get_room_name(@booking.room_id) %>
</p>

<p>
  <strong>Start:</strong>
  <%= @booking.start_time %>
</p>

<p>
  <strong>End:</strong>
  <%= @booking.end_time %>
</p>

<%= link_to 'Edit', edit_user_booking_path(@booking.user_id,@booking.id) %> |
<%= link_to 'Back', user_bookings_path(@booking.user_id)%>
